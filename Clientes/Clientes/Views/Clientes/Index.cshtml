@model IEnumerable<Clientes.Models.DTO.ClientesDTO>

@{
//ViewBag.Title = "Index";
}


<p>
    @* @Html.ActionLink("Agregar cliente", "Create", new { @class = "col-sm btn btn-danger" })*@
    <a class="col-sm btn btn-success " href="~/Clientes/Create">Agregar cliente</a>

</p>
<table class="table">
    <tr>
        <th>
           CodigoCliente
        </th>
        <th>
           Nombre cliente
        </th>
        <th>
            Ciudad Cliente
        </th>
        <th>
            Dirección
        </th>
        <th>
            Teléfono
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CodigoCliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreCliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CiudadCliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Direccion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Telefono)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |*@
            @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
            <a class="col-sm btn btn-primary " href="~/Clientes/Editar/@item.CodigoCliente">Editar</a>
            <a class="col-sm btn btn-danger " href="~/Clientes/Eliminar/@item.CodigoCliente">Eliminar</a>
        </td>
    </tr>
}

</table>
